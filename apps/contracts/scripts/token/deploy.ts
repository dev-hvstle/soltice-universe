import { ethers, upgrades } from "hardhat";

async function main() {
  //   const TimePotion = await ethers.getContractFactory("TimePotion");
  //   const timePotion = await upgrades.deployProxy(TimePotion, []);
  //   console.info("Deploying TimePotion...");
  //   await timePotion.waitForDeployment();
  //   console.info("TimePotion deployed to:", await timePotion.getAddress());
  //   const Usdt = await ethers.getContractFactory("USDT");
  //   const usdt = await upgrades.deployProxy(Usdt, []);
  //   console.info("\nDeploying Usdt...");
  //   await usdt.waitForDeployment();
  //   console.info("Usdt deployed to:", await usdt.getAddress());
}

main();
